services:
  - type: web
    name: chatbot-bili
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --port=$PORT wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3
    rootDir: .
